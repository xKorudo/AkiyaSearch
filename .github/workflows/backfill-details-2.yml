name: Backfill Details 2 & Deploy

# Second traffic/description backfill shard. Runs offset by 30 minutes from
# backfill-details.yml so both get fresh IPs at different times and together
# cover the full listing backlog (shard 0 vs shard 1).

on:
  schedule:
    - cron: "31 1-23/2 * * *"   # every 2 hours starting at 01:31 — offset from shard 0 at :01
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: akiya-pipeline
  cancel-in-progress: false

jobs:
  backfill-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright browser
        run: python -m playwright install --with-deps chromium

      - name: Restore listings DB
        uses: actions/cache@v4
        with:
          path: |
            akiya_v4.db
            geocache.json
          key: akiya-db-${{ github.run_id }}
          restore-keys: |
            akiya-db-

      - name: Backfill traffic & description (fresh IP, shard 1)
        run: python enrich_details.py 400 --shard 1
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}

      - name: Build static snapshot
        run: python export_static.py

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy public --project-name=akiya-finder --branch main
